import 'package:flutter/material.dart';

import 'fabrica_home.dart';

class HomePage extends StatelessWidget {
  static String tag = 'HomePage';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
